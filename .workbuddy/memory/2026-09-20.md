# 2026-09-20

## syncrelease.py：assets 同步到 GitHub Release(tag=database)
- 新增 `D:\workspace\qtsltools\syncrelease.py`，把 `assets/` 同步到 github.com/zunkun/qtsltools 的 Release「数据仓库」(tag=database)。
- 规则（用户确认）：update.json **每次强制 clobber 覆盖**；最新 APK(qtsl-v140.apk) 与 dataPackList 引用的 zip 线上已有则跳过、没有才传；旧版本 zip/apk 默认保留；about.json/privacy*.json/favicon.ico 等其他资源一律不动。
- 同名但大小不一致 → 自动 clobber 重传；`--force` 强制重传；`--prune-old` 删除线上"本地已不存在"的旧 apk/zip；`--dry-run` 只打印计划；`--token` 免登录。
- 同步清单以 update.json 为准（apk.name + dataPackList[].name）；本地未被引用的 apk/zip 会警告提示先跑 hashfile.py。
- gh.exe 自动定位：PATH `where gh` → `C:\Program Files\GitHub CLI\gh.exe` 等兜底（用户 winget 装完 gh 后旧终端 PATH 未刷新）。
- 坑：Windows UTF-8 模式下 subprocess text=True 解码 GBK 输出会 UnicodeDecodeError（`where gh` 场景），改 bytes+decode(errors=replace) 解决。
- 未完成：gh auth login 用户稍后自己做；登录后跑 `python syncrelease.py --dry-run` 预览，再去掉 --dry-run 正式同步。
- 11:58 用户完成 gh 登录(zunkun)，正式同步成功：覆盖 update.json、上传 qtsl-v140.apk + data-v106.zip；v108/v109 跳过；privacy.json/about.json/qtsl-v139.apk 原样保留。线上共 8 个资源，已用 API 复核。
- 12:12 修 git push 报错「src refspec main matches more than one」：根因是本地+远端都有个杂散标签 `main`（指向旧提交 c09b1c5），与分支 main 重名。处理：`git tag -d main` + `git push origin :refs/tags/main` 删掉该标签（可随时 `git tag main c09b1c5` 重建），再 `git push origin main` 成功（f00a1d2..22d613f，提交"feature: 自动上传文件"= syncrelease.py）。期间 origin/main 跟踪引用短暂异常（for-each-ref 读不到但文件在，疑 VS Code 后台 git 并发），fetch 后自行恢复，`git status` 已干净同步。

### v140 正式上线（2026-09-20 13:35）

- v140 = 本地打包产物（appqtsl/native/qtsl-native，含返回键/沉浸式状态栏修复，36MB）
- 流程：`hashfile.py`（apk.changelog 改为「修复返回键无法返回上一页、状态栏显示问题」）→ `syncrelease.py`（update.json + qtsl-v140.apk 覆盖上传）
- 线上验证：`gh api repos/zunkun/qtsltools/releases/tags/database` → qtsl-v140.apk 36829544 字节（05:34:22Z）；
  镜像拉到的 update.json = versionCode 140 / md5 feefc756400adb671454acd104175cb4，与本地文件一致
- v139（云打包）保留在线上，可回滚
